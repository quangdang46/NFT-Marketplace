export type ChainParams = {
  chain: string;
};

export type AddressParams = {
  address: string;
};

export type CollectionParams = {
  chain: string;
  address: string;
};

export type TokenParams = {
  chain: string;
  address: string;
  tokenId: string;
};
